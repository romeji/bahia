# Required only when the durable server-side portfolio is implemented.
DATABASE_URL=

# OKX Demo only. Never expose these with NEXT_PUBLIC_ prefixes.
OKX_DEMO_API_KEY=
OKX_DEMO_SECRET_KEY=
OKX_DEMO_PASSPHRASE=

# Must remain false until the production-readiness gates are passed.
ENABLE_LIVE_TRADING=false
